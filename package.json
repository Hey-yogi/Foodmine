{
  "name": "foodmine",
  "version": "1.0.0",
  "engines": {
    "node": ">=18 <19"
  },
  "scripts": {
    "start": "cd backend && npm start",
    "prebuild": "cd backend && npm run build",
    "build": "cd frontend && npm run build"
  }
}